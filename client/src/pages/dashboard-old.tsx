// Minimal stub to keep legacy page for reference and avoid TSX errors.
import React from 'react';

export default function DashboardOldStub(): JSX.Element {
  return <div style={{ display: 'none' }} />;
}
